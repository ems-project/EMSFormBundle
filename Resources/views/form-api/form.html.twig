{% trans_default_domain trans_default_domain %}
{% spaceless %}
    {
        "instruction": "{{ instruction|default('form') }}",
        "response": "{{ form(form)|escape('html') }}"
    }
{% endspaceless %}